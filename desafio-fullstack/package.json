{
  "name": "desafio-fullstack-root",
  "private": true,
  "scripts": {
  "dev:api": "cd backend && dotnet watch run --urls http://localhost:5080",
  "dev:ui": "cd frontend && npm run dev",
  "dev": "concurrently -k -n API,WEB -c green,cyan \"npm:dev:api\" \"npm:dev:ui\""
},
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^7.2.0"
  }
}
